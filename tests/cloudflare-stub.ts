export const env: {
  DB: any;
  BUCKET: any;
  LLM_API_URL?: string;
  LLM_API_KEY?: string;
  LLM_MODEL?: string;
  AGENT_DAILY_RUN_LIMIT?: string;
  AGENT_SITE_DAILY_RUN_LIMIT?: string;
} = { DB: null, BUCKET: null };
