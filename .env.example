# Optional server-side provider. Never commit actual credentials.
LLM_API_URL=
LLM_MODEL=
LLM_API_KEY=

# Reserved attempts per UTC day; cancelled/failed tasks still count.
AGENT_DAILY_RUN_LIMIT=20
AGENT_SITE_DAILY_RUN_LIMIT=100
