import test from 'node:test';import assert from 'node:assert/strict';import {checkCompanionInstallation} from './companion-install-check.mjs';
test('installation check invokes only bundled wrapper with current Node and exact pinned version',async()=>{const result=await checkCompanionInstallation({execute:async(command,args)=>{assert.equal(command,process.execPath);assert.ok(args[0].endsWith('/runtime/node_modules/@openai/codex/bin/codex.js'));assert.equal(args[1],'--version');return {stdout:'codex-cli 0.153.4\n'};}});assert.equal(result.installed,true);});
test('missing optional dependency and wrong version fail without leaking provider diagnostics or falling back',async()=>{for(const execute of [async()=>{throw Error('private provider detail');},async()=>({stdout:'codex-cli 0.1.0'})])await assert.rejects(checkCompanionInstallation({execute}),e=>e.message.includes('optional dependencies')&&!e.message.includes('private provider detail'));});
